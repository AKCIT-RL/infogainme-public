# Copy this file to .env and fill in your values.
# Required even when using local vLLM endpoints (the OpenAI client reads it).
OPENAI_API_KEY=your-key-or-any-string-for-local-vllm

# Required to download models from HuggingFace (gated models like Llama-3.1)
HF_TOKEN=hf_...
